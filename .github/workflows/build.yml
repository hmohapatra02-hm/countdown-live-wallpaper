target.set(Calendar.YEAR, 2026);
target.set(Calendar.MONTH, Calendar.SEPTEMBER);
target.set(Calendar.DAY_OF_MONTH, 22);
target.set(Calendar.HOUR_OF_DAY, 0);
target.set(Calendar.MINUTE, 0);
target.set(Calendar.SECOND, 0);long diff = target.getTimeInMillis() - System.currentTimeMillis();

long days = TimeUnit.MILLISECONDS.toDays(diff);
long hours = TimeUnit.MILLISECONDS.toHours(diff) % 24;
long minutes = TimeUnit.MILLISECONDS.toMinutes(diff) % 60;
long seconds = TimeUnit.MILLISECONDS.toSeconds(diff) % 60;handler.postDelayed(this, 1000);target.set(Calendar.YEAR, 2026);
target.set(Calendar.MONTH, Calendar.SEPTEMBER);
target.set(Calendar.DAY_OF_MONTH, 22);
target.set(Calendar.HOUR_OF_DAY, 0);
target.set(Calendar.MINUTE, 0);
target.set(Calendar.SECOND, 0);
long diff = target.getTimeInMillis() - System.currentTimeMillis();
long days = TimeUnit.MILLISECONDS.toDays(diff);
long hours = TimeUnit.MILLISECONDS.toHours(diff) % 24;
long minutes = TimeUnit.MILLISECONDS.toMinutes(diff) % 60;
long seconds = TimeUnit.MILLISECONDS.toSeconds(diff) % 60;
handler.postDelayed(this, 1000);
